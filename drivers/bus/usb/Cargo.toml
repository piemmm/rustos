[package]
name = "rustos-drv-bus-usb"
description = "RustOS xHCI USB host-controller driver (protocol layers)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
rustos-abi = { path = "../../../lib/abi", version = "=0.0.0" }

[dev-dependencies]
# End-to-end seam proof: the boot-protocol decoders polling reports
# straight off this crate's interrupt-IN transfer ring.
rustos-drv-input-usb-hid = { path = "../../input/usb_hid", version = "=0.0.0" }

[lints]
workspace = true
