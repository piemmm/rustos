[package]
name = "tairix-test-spawn-session-qemu-aarch64"
description = "QEMU integration test (plans/SPAWN.md SP3b): boot the aarch64 tairix-kernel pipeline on the virt board, spawn PID 1 (init) into EL0, have init launch the embedded Shell session program through the spawn syscall, and report PASS via ARM semihosting once both processes have been built and the session has run its banner and exited — proving the runtime spawn producer builds a second, hardware-isolated, concurrently-runnable process."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[lints]
workspace = true

# Freestanding "boot the aarch64 kernel pipeline, spawn PID 1, and let PID 1
# spawn the session" integration test — the runtime proof of
# `plans/SPAWN.md` SP3b. It reuses the production aarch64 boot pipeline
# through `tairix-kernel`'s library half (`boot_aarch64::boot`), which
# installs both the `InitSpawn` seam and the runtime `ProcessSpawn` producer
# + embedded-program registry into the `BootInfo` hand-off. After
# `kernel_core::kernel_main` emits `AuditEvent::BootCompleted` it spawns PID 1
# `init`, which writes its banner and then spawns `/System/Commands/elsh.app/Run`
# through the `spawn` syscall; the producer builds the session a fresh,
# isolated address space and admits it Ready, and both processes run. This
# bin only swaps the audit sink: the shared supervision witness recognising
# the session exit, `init` reaping, and the replacement image flips
# `qemu_exit::exit_success`. Keeping the
# QEMU-exit shortcut in a dedicated bin (not a Cargo feature on a production
# crate) prevents feature unification from leaking it into any production
# build (fail closed).

[[bin]]
name = "tairix-test-spawn-session-qemu-aarch64"
path = "src/main.rs"

[target.'cfg(all(target_arch = "aarch64", target_os = "none"))'.dependencies]
tairix-arch-aarch64 = { path = "../../../kernel/arch/aarch64", version = "=0.0.0" }
# The production aarch64 boot pipeline (with the `InitSpawn` seam and the
# runtime `ProcessSpawn` producer) lives in `tairix-kernel`'s library half;
# this vertical reuses it verbatim and only swaps the audit sink
# (no duplication).
tairix-kernel = { path = "../../../kernel/tairix-kernel", version = "=0.0.0" }
# The boot heap the `kernel_core::kernel_main` allocations (the leaked
# `KernelState`, the scheduler, the spawned address spaces) draw from.
tairix-kalloc = { path = "../../../lib/kalloc", version = "=0.0.0" }
tairix-log = { path = "../../../lib/log", version = "=0.0.0" }
# The supervision witness both ports' audit sinks fold their event stream
# through, so neither can drift from the other.
tairix-test-spawn-supervision = { path = "../spawn_supervision", version = "=0.0.0" }

[build-dependencies]
tairix-itest-harness = { path = "../harness", version = "=0.0.0" }
