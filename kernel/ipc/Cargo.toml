[package]
name = "tairix-kernel-ipc"
description = "TAIRiX kernel IPC: capability-checked message ports, shared memory, and asynchronous notifications."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# Stage 2.5 dependency rules (issue brief): kernel/ipc may depend only on
# kernel/{sync, mem, sched, sec} and lib/{abi, caps, log} (plus `lib/util`
# for the formatters extracted out of `kernel/sec` in this same stage,
# `AGENTS.md` §2.2). Nothing more is needed.
tairix-sync = { path = "../../lib/sync", version = "=0.0.0" }
tairix-kernel-mem = { path = "../mem", version = "=0.0.0" }
tairix-kernel-sec = { path = "../sec", version = "=0.0.0" }
tairix-abi = { path = "../../lib/abi", version = "=0.0.0" }
tairix-caps = { path = "../../lib/caps", version = "=0.0.0" }
tairix-log = { path = "../../lib/log", version = "=0.0.0" }
tairix-util = { path = "../../lib/util", version = "=0.0.0" }

# The §19.7 Bronze stateful model (`tests/proptest_model.rs`) drives the
# capability-checked port endpoint under randomised command sequences. Same
# pinned, already-audited version the other kernel crates use; dev-only.
[dev-dependencies]
proptest = { version = "=1.5.0", default-features = false, features = ["std"] }
# The §19.7 model drives its programs through the shared seed/budget runner
# (`tairix_fuzzseed::prop::drive`), so the seed selection, start-of-test
# seed log, and smoke/soak loop live in one place (`AGENTS.md` §2.2). Test
# scaffolding only; never in a TAIRiX build.
tairix-fuzzseed = { path = "../../tests/fuzzseed", version = "=0.0.0", features = ["proptest"] }


# Loom is only pulled in when the caller opts in with
# `RUSTFLAGS="--cfg loom" cargo test ...`. Mirrors `kernel/sync` and
# `kernel/sched`.
[target.'cfg(loom)'.dependencies]
loom = "0.7"
