[package]
name = "rustos-log"
description = "RustOS placeholder: structured logging."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# §19.4 hash-chained audit log. SHA-256 comes from the audited wrapper in
# `lib/crypto` (`AGENTS.md` §2.12 / §16.4 — no hand-rolled cryptography);
# this crate never names an upstream crypto crate directly.
rustos-crypto = { path = "../crypto", version = "=0.0.0" }
