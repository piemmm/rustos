[package]
name = "tairix-devmatch"
description = "TAIRiX deterministic hardware-node ↔ driver bind-table match resolution: the single match-policy definition shared by the user-space device manager and the kernel's interim in-kernel driver-candidate catalogue."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
tairix-abi = { path = "../abi", version = "=0.0.0" }
