[package]
name = "tairix-memguard"
description = "TAIRiX guard-region poison: the one definition of the sentinel byte a guarded memory region is filled with and the canary window a hot path verifies, shared by the slab guard, the kthread stack guard, and the boot-stack guard so the three can never drift apart. no_std, no dependencies, no unsafe."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
