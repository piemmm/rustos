[package]
name = "tairix-seat"
description = "TAIRiX arch-neutral seat model: the exclusive, owner-tracked, revocable lease on one display + keyboard + pointer, and its input-routing decision (plans/DISPLAY.md D1)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]

[lints]
workspace = true
