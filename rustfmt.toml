# RustOS formatting policy.
#
# Keep this file small. Anything not specified follows rustfmt defaults so the
# code matches what a contributor's editor produces without surprises.

edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
