## NAME

files — متصفح رسومي لنظام الملفات

## SYNOPSIS

`files [--desktop] [directory] [-h | -?]`

## DESCRIPTION

يفتح نافذة على سطح المكتب تعرض نظام الملفات، بدءًا من `directory`
المسمى في سطر الأوامر، أو من الدليل الشخصي للمستخدم الذي شغّل البرنامج
عندما لا يُسمّى أي دليل. يعرض الصف العلوي مسار الدليل الحالي؛ وتعرض
الصفوف التالية مدخلات الدليل، مع إبراز المدخل المحدد بلون التمييز
الخاص بالسمة النشطة. كل قراءة لدليل هي عملية سرد عادية تخضع لفحص
الأذونات تحت هوية المستخدم الذي شغّل البرنامج: الدليل الذي يتعذر
قراءته يُرفض ولا يُخمَّن أبدًا.

يُشغَّل المتصفح من زر `Files` الدائم في شريط المهام أو
بالاسم من الصدفة. وهو يتطلب جلسة رسومية قيد التشغيل: بدونها تكون قناة
النافذة غير قابلة للوصول، فيبلغ المتصفح عن الرفض في مجرى الخطأ
القياسي ثم ينتهي.

تُدار النافذة بلوحة المفاتيح: `أسفل` و`أعلى` لتحريك التحديد، و`Enter`
لفتح الدليل المحدد، و`Backspace` للصعود إلى الدليل الأب. إغلاق النافذة
من سطح المكتب ينهي المتصفح.

يُعامل المعامل `directory` كمدخل غير موثوق: يجب أن يكون مسارًا مطلقًا
ضمن حد طول المسار في النظام، ويجب أن يكون كل جزء من أجزائه اسم دليل
حقيقيًا — و`.` و`..` ليسا كذلك، فلا يمكن لكتابة أن تعني موضعًا غير ما
تُقرأ به أبدًا. والدليل الذي يخالف أيًّا من تلك القواعد، أو الذي لا
يستطيع المستخدم الذي شغّل البرنامج سرده، يُرفض مع ذكر السبب في مجرى
الخطأ القياسي، وتُفتح النافذة عندئذ على الدليل الشخصي، فلا يترك
الوسيط الخاطئ المستخدم بلا نافذة أبدًا. ويُرفض المعامل الثاني رفضًا
تامًا بدل تجاهله.

## OPTIONS

- `--desktop` — التشغيل كمكوّن مدير الملفات الخاص بسطح المكتب: خانة دائمة
  على شريط الأيقونات تعرض أماكنك ووحدات التخزين المركّبة، دون نافذة حتى
  تُطلب، ودون طريقة للخروج. تمرّر جلسة سطح المكتب هذا الخيار عند البدء؛
  وتسمية `directory` معه مرفوضة، لأن المكوّن لا يفتح نافذة لوضعه فيها.
- `-h, -?` — عرض المساعدة القصيرة لهذا الأمر نفسه والخروج.

## EXIT STATUS

صفر بعد إغلاق سليم، أو بعد عرض المساعدة القصيرة؛ و`2` عندما لا يُفهم
سطر الأوامر؛ وقيمة غير صفرية فيما عدا ذلك عندما تُرفض قناة النافذة أو
منطقة الإطارات المشتركة أو السرد الأولي للدليل (يُذكر السبب في مجرى
الخطأ القياسي).
