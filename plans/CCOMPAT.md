# CCOMPAT.md — A C-callable `abi-v1`: full `lib/abi` header surface, syscall stubs, and crt0

This is a staged build plan for making the whole RustOS user/kernel ABI
(`abi-v1`) callable from programs **not written in Rust** (C first, then any
language with a C FFI). It is **binding under `AGENTS.md`**; read `AGENTS.md`
and `PLAN.md` first. Every rule in both applies here without exception. This
plan exists because the charter requires a new OS-provided interface surface
and a new curated shared-library class to be proposed and approved in a plan
file *before* any of it is built (`AGENTS.md` §9, §16.4, §15.2).

## 0. Scope and decisions (binding for this plan)

- **RustOS stays Rust-only (`AGENTS.md` §1).** We do not add C, C++, or a C
  toolchain to the RustOS codebase. This plan makes the ABI *consumable* by
  non-Rust programs; it does not make RustOS itself contain C. Hosting a C
  program is a different thing from authoring the OS in C, and §16.5 already
  anticipates non-Rust third-party apps.
- **The whole of `lib/abi` is the C surface, not just the syscalls.**
  `lib/abi` is the single source of truth for *every* type a program exchanges
  with the kernel and with system services — not only `syscalls.rs`. The
  C-language view therefore covers the entire crate: `appinfo`, `capability`,
  `driver/*`, `error`, `input`, `ipc`, `manifest`, `random`, `rxe`, `syscall`,
  `syscalls`, `sysinfo`, `time`, `stdinfo`. Every public `#[repr(C)]` /
  `#[repr(transparent)]` / explicit-primitive type, every constant, and every
  enum discriminant gets a C declaration. The existing `cargo xtask c-header`
  (syscall numbers + error codes + capability ids + syscall prototypes) is the
  seed; this plan grows it to the full crate.
- **`lib/abi` is for third-party programs, not just the OS.** The generated
  header and the runtime that backs it are a *public developer surface*:
  third-party C programs link against them exactly as OS-bundled programs do.
  This is why the header lives in its own top-level `include/` folder
  (shippable to developers independently, `AGENTS.md` §3) and why the runtime
  that exports the entry points is a curated `/System/Libraries/` class
  (§16.4), dynamically linked by every consumer so one security update covers
  them all.
- **Generated, never hand-written (`AGENTS.md` §2.2, §9).** The header is a
  *view* of `lib/abi`, regenerated by `cargo xtask c-header --write` and
  verified (fails closed on drift) by `cargo xtask c-header` inside
  `cargo xtask ci`. There is never a parallel, hand-maintained C definition.
- **Roll our own (`AGENTS.md` §2.12).** The header generator, the syscall stub
  runtime, and crt0 are all first-party. We do **not** take a load-bearing
  dependency on `cbindgen` or an external libc for a security-critical surface.
- **`abi-v1` is NOT frozen.** RustOS has not shipped a release, so `abi-v1` is
  still mutable. It becomes immutable only at the first release; until then,
  changing a `lib/abi` type is allowed and simply requires regenerating the
  header (the drift guard enforces that). After release, new behaviour ships
  as `abi-v2` (`AGENTS.md` §9).
- **Security is the point, not an afterthought (`AGENTS.md` §5, §7, §9, §19).**
  Third-party native code is treated as **potentially hostile and/or poorly
  written**. See §4 below; it is binding for every stage.
- **No stubs (`AGENTS.md` §15.1).** Each stage ships code **plus** tests
  **plus** docs and is only "done" when the whole-project gate (§7) is green.

## 1. Native-target scope

The C-callable surface targets the three **native** Tier-1 targets:
`x86_64-unknown-none`, `aarch64-unknown-none`, `riscv64gc-unknown-none-elf`.
`wasm32-unknown-unknown` has no trap instruction and a different toolchain /
linking story (the browser-host `extern "C"` glue, `PLAN.md` Stage 2/2.6); the
header's *type* declarations are still valid there, but the syscall-stub
runtime and crt0 are explicitly out of scope for wasm in this plan and the
docs say so.

## 2. Type-mapping and symbol rules (frozen alongside `abi-v1`)

These rules are the contract the generator implements; they are fixed so the
header means the same thing on every native Tier-1 target.

- **Fixed widths from `<stdint.h>`.** No host `size_t`/`int` assumptions:
  - 32-bit signed / `Errno` → `int32_t`; 32-bit unsigned → `uint32_t`.
  - capability id → `uint16_t`; 64-bit / opaque handle / IPC endpoint →
    `uint64_t`.
  - byte length (`Len`) → `uintptr_t` (register-width: it must address the
    caller's whole space); user pointer → `void *`.
- **Little-endian wire layout.** The encode/decode helpers in `lib/abi`
  (`le.rs`, the `to_le_bytes`/`from_bytes` pairs) are little-endian; the header
  documents this and the byte images do not depend on host endianness.
- **Stable C names.** Rust newtypes and enums get a namespaced C spelling:
  constants as `RUSTOS_<AREA>_<NAME>`, enum discriminants pinned to their Rust
  value, structs as `rustos_<snake_case>_t`. The names are part of the frozen
  surface once `abi-v1` ships.
- **Stable export symbols.** Each syscall is exported as `rustos_sys_<name>`
  (e.g. `rustos_sys_ipc_send`). The Rust stub pins each with
  `#[export_name = "rustos_sys_<name>"]` (or `#[unsafe(no_mangle)]` where the
  symbol equals the item name) so the compiler does not mangle it. `extern "C"`
  alone only fixes the *calling convention*; it does **not** stop mangling —
  hence the explicit pin.
- **No panic across the boundary (`AGENTS.md` §2.9).** Every `extern "C"`
  entry point is panic-free: errors are returned as `int32_t` `RUSTOS_E_*`
  codes (an unwind across an FFI boundary is undefined behaviour and is
  forbidden).

## 3. Work breakdown (stages)

Each stage is a reviewable chunk. **At the end of each stage, write the
continuation prompt for the next stage to `.junie/next-ccompat-prompt.md`**
(overwrite each time), recording what landed and the exact next work, in the
style of the other `.junie/next-*-prompt.md` files. Do not start a stage
before its predecessor is green on the whole-project gate (§7).

### Stage CC1 — Full `lib/abi` C header surface

**Status: not started.**

**Deliverables**
- Grow the `tools/xtask` generator (`commands/c_header.rs`) from the current
  syscall/errno/capability/syscall-prototype seed to the **whole** `lib/abi`
  public surface: every module's `#[repr(C)]` structs, `#[repr(uN)]`/`iN` enums
  (discriminants pinned), `*_MAGIC`/`*_MAX`/`*_LEN` constants, and the
  `Time64`/`Duration64`, `IpcMessageHeader`, `ProcessRecord`,
  `SysinfoRequestHeader`, `Rxe`/`LoadHeader`, driver-trait POD types, etc.
- Organise the output as one header per `lib/abi` module under
  `include/rustos/` (e.g. `rustos_time.h`, `rustos_ipc.h`, `rustos_sysinfo.h`)
  plus the existing umbrella `rustos_abi.h` that `#include`s them, so a
  developer can pull in exactly what they need.
- Values are read from `lib/abi` (discriminants, constant values, struct field
  order/size) — never re-typed — so the header can never disagree with the
  source of truth (§2.2). Where Rust offers no run-time reflection (e.g. enum
  variant *names*), the only thing that lives in the generator is the C
  spelling, pinned by an in-module test against the frozen Rust value.
- Extend the drift guard: `cargo xtask c-header` regenerates the full set in
  memory and compares byte-for-byte with the committed `include/` tree, failing
  closed on any mismatch; `--write` regenerates. Already wired into
  `cargo xtask ci`.

**Tests** — per-module generation tests; a test that every `pub` `#[repr(C)]`
type in `lib/abi` is represented in the header (guards against a new type
silently escaping the C surface — the same "dense/complete" discipline the
existing `errno_table_matches_the_frozen_enum` test uses); the byte-for-byte
drift test.

**Docs** — expand `docs/src/abi/c-abi.md` (type-mapping table, per-module
header list, endianness, the "not frozen yet" note); rustdoc on the generator.

### Stage CC2 — `lib/abi-sys`: the C-callable syscall stub runtime

**Status: not started.** **Depends on** the per-architecture trap layer
(`PLAN.md` Stage 6+; the kernel entry already exists per arch in
`kernel/arch/*/src/syscall_entry.rs`).

**Deliverables**
- A new crate (`lib/abi-sys`, `rustos-abi-sys`) providing one `extern "C"`,
  export-name-pinned `rustos_sys_<name>` function per `abi-v1` syscall that
  issues the actual trap (`syscall` / `svc` / `ecall`) on the three native
  targets. The per-arch trap instruction is the §1-sanctioned assembly
  carve-out; each such file carries the §1 justification header and a
  `// SAFETY:` block (§2.10).
- Panic-free at the boundary (§2.9): every stub marshals registers and returns
  an `int32_t` `RUSTOS_E_*` code; no unwind crosses `extern "C"`.
- This crate is the implementation behind the CC1 header. It is an
  **OS-provided shared library** (the new §16.4 class — see §5 of this plan),
  dynamically linked by consumers; OS-bundled apps must not vendor it.

**Tests** — host-buildable tests of the marshalling/return-code logic with the
trap injected behind a seam (mirroring how the kernel side is host-tested); a
QEMU integration test per native target that traps into a stub kernel and
checks register marshalling; the stub crate is enrolled where applicable.

**Docs** — `docs/src/abi/c-abi.md` updated; crate `README.md` with a stability
tier; `AGENTS.md` §3 + `PLAN.md` registration (§6).

### Stage CC3 — crt0: C program startup/teardown

**Status: not started.** **Depends on** CC2 and the loader (`PLAN.md` Stage 6
bundle loader / dynamic-loader policy).

**Deliverables**
- A first-party, per-native-target crt0 (program entry/exit runtime) that a
  non-Rust binary uses as its startup object: kernel entry trampoline → set up
  the C runtime environment (stack alignment per the platform C ABI, argument /
  environment / startup-vector marshalling from the kernel's process-spawn
  contract), call the program entry point, and route its return through the
  `exit` syscall. No global mutable state, no busy-wait, fail-closed (§2.1,
  §2.9).
- crt0 enforces the `rxe` hardening invariants (`AGENTS.md` §9, §19.2) for the
  hosted program: position-independent (PIE), `R`/`RX`/`RW`-only segments (no
  `RWX`), the syscall-hash-derived CFI tag, and the stack-canary / shadow-stack
  posture required of the `unsafe` startup path. A C binary that cannot satisfy
  these is refused at load time, not patched up at runtime.
- Define exactly what the kernel hands a freshly spawned process (the
  argument/startup-vector block) as an `abi-v1` type in `lib/abi` (so crt0 and
  the kernel share one definition, §2.2) and surface it in the CC1 header.

**Tests** — host tests of the startup-vector parsing (untrusted layout → typed,
fail-closed); a QEMU test that a crt0-linked program starts, reads its
arguments, and exits with the expected code; a test that a `RWX`-segmented or
non-PIE image is refused.

**Docs** — `docs/src/abi/c-abi.md` "building and linking a C program" section;
crt0 crate `README.md`.

### Stage CC4 — Loader / bundle integration for native programs

**Status: not started.** **Depends on** CC2, CC3, and `PLAN.md` Stage 6
dynamic-loader policy.

**Deliverables**
- Confirm and, where needed, extend the `rxe` loader and the §16.4/§16.5
  dynamic-loader policy so a C-compiled, PIE, capability-manifested bundle
  loads and resolves the `rustos_sys_*` runtime **only** from the curated
  `/System/Libraries/` class or the app's own bundle `Libraries/` — never an
  arbitrary path (§16.4). The CFI tag / syscall-hash check (§19.2) applies to C
  binaries identically.
- No new ambient authority: a hosted C program receives exactly the capability
  set computed as the intersection of its signed `AppInfo` manifest request and
  the launching user's grants (§4, §16.5). The stub runtime is not a privileged
  path — see §4 of this plan.

**Tests** — a bundle-loader test that resolves the runtime from
`/System/Libraries/` and rejects an out-of-tree shared-library reference; a
capability-intersection test for a C bundle; a load-refusal test for a
manifest/syscall-hash mismatch.

**Docs** — `docs/src/abi/c-abi.md` bundle/loader section; cross-link §16.4/§16.5.

### Stage CC5 — End-to-end C program + fuzzing

**Status: not started.** **Depends on** CC1–CC4.

**Deliverables**
- A minimal in-tree C program (built by an audited, version-pinned, checksummed
  toolchain wrapper under `tools/`, in the spirit of
  `tools/mkimage/src/extern_tools.rs`, §12 — no unaudited shell-out) that
  includes `include/rustos/…`, links the stub runtime + crt0, and exercises a
  representative slice of `abi-v1` (a syscall round-trip, a `Time64` value
  across §21's pre-1970 / post-2038 boundaries, an `sysinfo` request/response,
  an `ipc` header) under QEMU. This proves the header, the runtime, and crt0
  agree with the Rust side end-to-end.
- Fuzz/regression: the `lib/abi` decoders are already fuzzed (§19.6); ensure the
  startup-vector and any new CC3 decoder are enrolled, and add a crash-corpus
  regression test for each.

**Tests** — the QEMU C-compile-and-run integration test; the §21 timestamp edge
cases asserted from the C side; the fuzz registrations.

**Docs** — a "calling RustOS from C" guide page under `docs/src/abi/`.

## 4. Security posture — third-party native code is hostile (binding)

Native third-party code is assumed **potentially hostile and/or poorly
written**. Every stage upholds this; it is as non-negotiable as `AGENTS.md` §2.

- **The kernel re-checks everything; the stub is not a bypass (`AGENTS.md`
  §5.4).** The `rustos_sys_*` runtime only marshals registers and traps. Every
  capability check, every input validation, and every "fail closed" decision
  happens kernel-side, on the far side of the trap, exactly as for a Rust
  caller. A C program reaches no syscall, IPC endpoint, or filesystem op that a
  Rust program could not, and gains no authority by virtue of being C.
- **No ambient authority (`AGENTS.md` §4, §16.5).** A hosted program runs with
  the intersection of its signed manifest request and the launching user's
  grants. The C surface adds no implicit capability.
- **Exploit-mitigation defaults apply to C binaries (`AGENTS.md` §19.2).** PIE,
  W^X (no `RWX` segment), the syscall-hash CFI tag, and stack canaries are
  enforced at load by crt0/the loader — a non-conforming C binary is refused,
  not accommodated.
- **Untrusted input stays untrusted (`AGENTS.md` §19.5/§19.6).** Anything the
  runtime or crt0 parses that originates outside the kernel (the startup vector,
  any decoded `abi-v1` byte image) is treated as untrusted: fail-closed
  decoders, no panic, fuzzed. Parser-heavy work a C program triggers still runs
  under the minimum-capability sandbox posture where §19.5 requires it.
- **No panic / no UB across the boundary (`AGENTS.md` §2.9).** An unwind across
  `extern "C"` is UB; every entry point is panic-free and returns typed errno
  codes.
- **No post-install network fetch, signed bundles only (`AGENTS.md` §19.3,
  §16.5).** The C surface does not introduce a code-fetch path; programs arrive
  as signed `rxe`/`.app` bundles through the normal install/update path.

## 5. New curated `/System/Libraries/` class (charter amendment, §16.4)

The runtime that exports `rustos_sys_<name>` and provides crt0 is a new
OS-provided shared-library class. Per `AGENTS.md` §16.4, adding a class
"requires an update to this list **and** to `PLAN.md`"; this plan drives both:

- **Class:** *System runtime / C ABI* — the minimal libc-equivalent that lets a
  non-Rust program call `abi-v1`: the `rustos_sys_*` syscall stubs (CC2) and the
  crt0 startup/teardown (CC3). It is deliberately minimal (no sprawling libc):
  it marshals to the kernel and starts/stops the program; everything else a
  program wants it brings itself (§16.5) or links from another curated class.
- It is **dynamically linked** like every other curated library, so one
  security update covers every consumer; OS-bundled programs must not vendor
  it.
- It is **not** a privileged path: §4 above governs.

`AGENTS.md` §16.4 gains this class and §9 references this plan; both edits land
with this plan (they are documentation, in the same change, §13).

## 6. Definition of done (per stage and overall)

Per `AGENTS.md` §7, over the **whole project** (never `-p`), and quote the
output:
1. `cargo fmt --all` (verify `cargo fmt --all --check`).
2. `cargo xtask ci` (clippy `-D warnings`, deps-check, cfg-check, test matrix,
   docs-check, `cargo deny`, the `--quick` fuzz/proptest gates, model-check,
   spec-review, abi-check, **c-header**).
3. `cargo xtask fuzz --secs 5`.
4. Anything else `.github/workflows/ci.yml` runs (e.g. `tools/ci/soak.sh`).

Any failure found — new or pre-existing — is fixed or reverted before the stage
is done (§2.5, §7). Update `PLAN.md` and this file's stage statuses as stages
advance, and refresh `.junie/next-ccompat-prompt.md` for the next chunk.

## 7. Charter cross-references

§1 (Rust-only OS; hosting C ≠ authoring in C), §2.2 (one definition, no
duplication), §2.9 (no panic / fail closed), §2.12 (roll our own; no cbindgen /
libc dependency), §3 (`include/` + `lib/*` layout), §4 (no ambient authority),
§5.4 (capability checks kernel-side), §6 (lib registration + stability tier),
§7 (whole-project gate), §9 (versioned ABI; `abi-v1` immutable only once
shipped — not yet), §12 (audited extern-tool wrappers), §13 (docs in same
commit), §15 (AI-agent rules), §16.4 (curated shared libs; the new System
runtime / C ABI class), §16.5 (bundles, signed manifests, capability
intersection), §19.2 (W^X / PIE / CFI / canaries), §19.3 (supply chain), §19.5
/ §19.6 (parser sandbox + fuzzing), §21 (`Time64` edge cases).
